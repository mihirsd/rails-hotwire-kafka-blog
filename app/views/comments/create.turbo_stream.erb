<%= turbo_stream.append "#{dom_id(@post)}_comments_list", partial: "comments/comment", locals: { comment: @comment } %>
<%= turbo_stream.replace "#{dom_id(@post)}_comment_form", partial: "comments/form", locals: { post: @post, comment: Comment.new } %>
