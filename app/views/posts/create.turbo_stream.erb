<%= turbo_stream.prepend "posts", partial: "posts/post", locals: { post: @post } %>
<%= turbo_stream.replace "post_form", partial: "posts/form", locals: { post: Post.new } %>
<%= turbo_stream.remove "no_posts_message" if Post.count == 1 %>
